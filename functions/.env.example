# Firebase Admin SDK - Get service account key from Firebase Console
# Go to: Project Settings > Service Accounts > Generate New Private Key
# Save the JSON file and set the path here:
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# VIC Credentials (for session login)
VIC_EMAIL=your-vic-email@example.com
VIC_PASSWORD=your-vic-password
